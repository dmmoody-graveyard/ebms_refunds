<h1>Groups</h1>

<ul>
  <% @groups.each do |group| %>
    <li><%= group.group_number %> | <%= group.name %></li>
  <% end %>
</ul>

<p>
  <%= link_to 'Add a new group', new_group_path(@group) %>
</p>
