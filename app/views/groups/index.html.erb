<h1>Groups</h1>

<ul class='lead list-unstyled'>
  <% @groups.each do |group| %>
    <li><%= group.group_number %> | <%= link_to group.name, group_path(group) %></li>
  <% end %>
</ul>

<p>
  <%= link_to 'Add a new group', new_group_path(@group) %>
</p>
